{
  "name": "order_processor",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "docker:local:up": "docker compose -f docker-compose.local.yml up --build -d",
    "docker:local:down": "docker compose -f docker-compose.local.yml down ",
    "dev:api": "ts-node src/server.ts",
    "start:api": "node dist/server.js",
    "start:worker": "node dist/worker.js",
    "start:recovery": "node dist/recovery.js",
    "db:migrate": "npx drizzle-kit generate --dialect=postgresql --out ./src/lib/sql/migrations --schema=./dist/lib/sql/models/*.js",
    "db:studio": "npx drizzle-kit studio"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.2.3",
    "drizzle-orm": "^0.45.1",
    "express": "^5.2.1",
    "pg": "^8.17.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.0.9",
    "@types/pg": "^8.16.0",
    "drizzle-kit": "^0.31.8",
    "typescript": "^5.9.3"
  }
}
